This file requires credentials for google classroom api downloaded and re named as "credentials.json"

Upon first run, a token file will be generated that should be uploaded along with everything else to server provider 